<h1> Disco Articles </h1>

<% @articles.each do |article| %>
  <div id="article-<%= article.id %>">
  <h3><%= link_to article.title, article_path(article) %></h3>
  <p><%= article.preview %></p>
  </div>
  <br>
<% end %>
